[project]
name = "reprisentre-ingest"
version = "0.1.0"
description = "Daily ingest jobs for Reprisentre listing sources"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "beautifulsoup4>=4.12",
    "supabase>=2.5",
]

[tool.setuptools]
packages = ["common", "sources"]
